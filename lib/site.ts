/** Site-level constants. The contact address backs the book "reserve" action
 *  (payments are M6; reserve/notify launches first — see BUILD_ROADMAP.md). */
export const CONTACT_EMAIL = process.env.NEXT_PUBLIC_CONTACT_EMAIL || "editor@gangatirtham.org";
